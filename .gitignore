# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# 런타임 데이터(로컬 스토리지/SQLite 카탈로그)
/data/
.run/

# 환경 파일 — 실 자격증명 파일(.env)은 커밋 금지.
# 템플릿(.env.example / .env.production)은 자리표시자만 담아 추적 허용.
.env
.env.*
!.env.example
!.env.production

# 운영 환경 자격증명 — 절대 커밋 금지(공개 저장소)
docs/운영환경.txt
*운영환경*
*_secrets*


# 대용량 샘플 데이터(생성기로 재생성 가능) — generate_samples.py 는 추적
/sample_data/production_10mb.*

# OS/IDE
.DS_Store
.idea/
.vscode/
