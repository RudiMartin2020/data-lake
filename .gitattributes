# 기본: 텍스트 자동 정규화
* text=auto eol=lf

# 셸 스크립트는 Linux(centos9)에서 실행 → 반드시 LF 유지(CRLF 시 실행 실패)
*.sh text eol=lf

# 바이너리/대용량 정적 에셋은 변환 금지
*.png binary
*.parquet binary
*.xlsx binary
app/static/*.js binary
app/static/*.css binary
