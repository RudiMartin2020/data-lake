# --- 코어 (필수, 단일 프로세스 테스트에 충분) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
# 관측성(Prometheus /metrics) · DLQ 재처리 큐 깊이(redis)
prometheus-client>=0.20

# --- 테이블 포맷 / 쿼리 엔진 (Apache Iceberg + PyArrow + DuckDB) ---
pyiceberg[sql-postgres]>=0.10
pyarrow>=15
# DuckDB: PyIceberg 프루닝 결과(PyArrow)를 in-memory SQL 집계 (확장 불필요)
duckdb>=0.10

# --- 선택 백엔드 (운영 전환 시) ---
# STORAGE_BACKEND=minio  (Iceberg warehouse 를 MinIO/S3 에 저장)
minio>=7.2
# TASK_BACKEND=celery
celery>=5.3
redis>=5.0
# CATALOG_BACKEND=postgres
psycopg[binary]>=3.1
psycopg-pool>=3.2

# --- 테스트 ---
httpx>=0.27
pytest>=8.0

# --- 샘플 데이터 생성기(선택, sample_data/generate_samples.py) ---
openpyxl>=3.1
